# Tue. 11.08.2026
- utils as a package
  - configured the project to have three independent projects: frontend, backend, and utils
- put up the http server
  - issue: route '/' issue, used the forward-slash in the initial router use in `app.ts` this caused the router to not route properly
- created github repo
  - configured github repo as well w/ some issues i have to do as well

# Wed. 12.08.2026
Focus: DB & API design
## DB design
- [x] DB design
  - [x] ERD diagram
    - [x] 3 collections
    - [x] relations
  - [x] Mongoose Schema
    - [x] Validation: 
      - required fields (entire user table, titles, descriptions, relational ids)
      - status column in Project is an ENUM
      - character length limit for username, title, and descriptions
### ERD diagram:
#### Relationships:
**A.** a *User* can own many *Project*s, but a *Project* can only be owned by one *User*<br>
**B.** a *User* can make many *Update*s, but an *Update* can only belong to one *User*<br>
**C.** a *Project* can have many *Update*s, but an *Update* can only belong to one *Project*<br>

#### Table Columns
**objectId**s are automatically generated by mongoDB

| **USER**    |
|-------------|
| username    |
| password    |
| *object-id* |

---

| **PROJECT**   |
|---------------|
| title         |
| description   |
| status        |
| owner-id      |
| contributors  |
| *object-id*   |

- *owner* objectId of a *USER*
- *contributors*
  - either a list of objectIds of *USER*s
  - or a lookUp on the *UPDATE* table with either project or contributor id set
---

| **UPDATES**    |
|----------------|
| title          |
| description    |
| project-id     |
| contributor-id |
| *object-id*    |

## API design
- [ ] full CRUD operation for main entity
- [ ] min. 2 relational endpoints that join/reference multiple collections
- [ ] custom endpoint (filtering, aggregation, etc) --> use URL params/queries
- [ ] handle errors consistently --> return appropiate HTTP codes
- [ ] use the MVC pattern
- [ ] input validation for POST/PUT routes
### Main Entity: Project
- main entity --> Project
- all CRUD ops require the user to be logged in
- throw 401 if user not logged in

### **CREATE**
- POST `/api/projects`
  - requires request body
  - returns 201
  - returns 400 if no/insufficient req. body
  - returns 409 if project title not unique
###     **READ**
- GET `/api/projects/:id`
  - --> specific project
- GET `/api/projects` or `/api/projects/:userId`
  - for the first one: use Gunnars method of a logged in User
  - otherwise use the second method
- returns 200
- returns 400 if invalid id
###   **UPDATE**
- PATCH `/api/projects/:id`
  - requires request body
  - returns 200
  - returns 400 if no/insufficient req. body
  - returns 400 if invalid id
###     **DELETE**
- DELETE `/api/projects/:id`
  - returns 204/200
  - returns 400 if invalid id
  - returns 404 if id couldn't be found --> already deleted/never existed
### WIP
**Done**
- read & create queries are done
- read models are done
- getProjectById route & controller are done
- move isUUID to utils
**ToDo**
- *create* modelsX
- complete *read* & *create* routesX
- implement *delete* endpointX
- implement *update* endpointX

### ErrorHandling & Types
- implemented the `asyncHandler` and `errorHandler` from my Fullstack Project
- implemented Types
  - httpCodes
  - DTO
  - ApiError Class
  - request shape
  - response shape

### Other
- cleaned up BE tsconfig.json
- removed OBJECT_ID type
- 
## Links
- [Mongoose Enums](https://stackoverflow.com/questions/29299477/how-to-create-and-use-enum-in-mongoose)
- [1-to-Many Relationships in Mongoose Schema](https://stackoverflow.com/questions/34985846/mongoose-document-references-with-a-one-to-many-relationship)
  - [Mongoose Populate](https://mongoosejs.com/docs/populate.html)
- [Max String Length for Mongoose Schema](https://stackoverflow.com/questions/28829912/mongoose-schema-set-max-length-for-a-string)
- 


# Thur. 13.08.2026
- [ ] user auth --> register, login, auth-middleware